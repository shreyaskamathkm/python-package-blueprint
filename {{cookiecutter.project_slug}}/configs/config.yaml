defaults:
  - _self_
  - model: default
  - data: default
  - optimizer: adam
  - scheduler: step_lr

training:
  epochs: 10
  batch_size: 32
  seed: 42
  device: "cuda"

experiment_name: "default_experiment"
